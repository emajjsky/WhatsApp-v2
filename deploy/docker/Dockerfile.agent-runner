ARG PYTHON_IMAGE=python:3.12-slim

FROM ${PYTHON_IMAGE}

WORKDIR /app

ENV PYTHONDONTWRITEBYTECODE=1
ENV PYTHONUNBUFFERED=1

COPY agent_runner /app/agent_runner

RUN python -m compileall /app/agent_runner

EXPOSE 8090

CMD ["python", "-m", "agent_runner.app"]
