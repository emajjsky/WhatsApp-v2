# 交付状态说明

更新日期：2026-03-28

## 当前交付结论

当前仓库已经能作为本地可运行版本交付，适合继续迭代，不再是半成品壳子。

客户当前可直接使用的页面只有：

- 账号接入
- 对话查看
- 导出中心

## 已交付内容

### 1. 运行环境

- Docker 一键启动可用
- Web、API、PostgreSQL、agent_runner 都可以拉起
- 前端构建通过
- Go 侧编译和测试通过

### 2. 账号与会话

- 创建账号
- 发起配对
- 查询状态
- 退出登录
- 删除账号

### 3. 聊天与归档

- 实时消息入库
- 聊天列表查询
- 消息历史查看
- 媒体附件落地

### 4. 导出

- 创建导出任务
- 查询任务状态
- 每个会话分别生成文件
- 导出文件直接下载到本地

### 5. 客户前端收口

- 首页总览已去掉
- Agent 入口已隐藏
- 账号页与聊天页已按高密度使用场景重排

## 未完全交付内容

### 1. 断线期间消息绝对补齐

当前已经做到“尽量补齐”，但还不能承诺：

- 所有断线期间消息百分百补回
- 所有群消息百分百解密成功

### 2. 独立导出 worker

导出任务目前仍由 API 进程内异步执行。

### 3. 客户侧 Agent 页面

Agent 后端与服务仍在，但客户侧入口已隐藏，不属于当前交付范围。

## 已做过的验证

- `go test ./internal/... ./cmd/...`
- `cd web && npm run build`
- `docker compose -f deploy/docker/docker-compose.all.yml up -d --build`
- 导出产物本地保存验证通过

## 交付建议

如果继续往前做，推荐顺序：

1. 继续增强断线消息补齐
2. 拆分导出 worker
3. 客户确认后再评估 Agent 是否恢复前端入口
