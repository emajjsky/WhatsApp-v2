# 交付状态说明

更新日期：2026-08-22

## 当前交付结论

当前仓库已经可以作为 Windows 桌面客户端测试版本交付，云端鉴权后台与本地 WhatsApp 数据链路可以分开运行。当前版本为 `0.1.43`。

客户当前可直接使用的页面只有：

- 账号接入
- 对话查看
- 导出中心

## 已交付内容

### 1. 运行环境

- Docker 一键启动可用
- Web、API、PostgreSQL、agent_runner 均可拉起
- 前端构建通过
- Go 侧编译和测试通过
- 已补充 macOS / Linux 启停脚本

### 2. 账号与会话

- 创建账号
- 发起配对
- 查询状态
- 退出登录
- 删除账号

### 3. 聊天与归档

- 实时消息入库
- 聊天列表查询
- 消息历史查看
- 媒体附件落地
- 页面实时刷新

### 4. 导出

- 创建导出任务
- 查询任务状态
- 每个会话分别生成文件
- 导出文件直接下载到本地
- 支持批量下载

### 5. 客户前端收口

- 首页总览已移除
- Agent 入口已隐藏
- 账号页、聊天页、导出页已按客户当前使用场景重排

### 6. 跨平台说明

- Windows / macOS 使用当前一键部署方案时，都需要先安装并启动 Docker Desktop
- Linux 可使用 Docker Engine + Docker Compose，或使用 Docker Desktop
- `.env` 需由 `.env.example` 复制生成
- 当前 `.ps1` 不是废文件，删除会导致 Windows 一键启动失效

### 7. 本轮网络修复

- 普通 Clash 代理到账号独立静态 SOCKS5 代理的链式拨号已验证。
- 修复 HTTP CONNECT 成功后误读响应体导致长连接卡住的问题。
- 增加 SOCKS5 用户名密码握手和详细拒绝原因。
- `desktop/scripts/prepare-icon.ps1` 未被构建或部署流程引用，列为清理项；其他脚本均有明确入口，不删除。

## 未完全交付内容

### 1. 断线期间消息绝对补齐

当前已做到“尽量补齐”，但还不能承诺：

- 所有断线期间消息百分百补回
- 所有群消息百分百解密成功

### 2. 独立导出 worker

导出任务目前仍由 API 进程内异步执行。

### 3. 客户侧 Agent 页面

Agent 后端与服务仍在，但客户端入口已隐藏，不属于当前交付范围。

## 已做过的验证

- `go test ./internal/... ./cmd/...`
- `cd web && npm run build`
- `docker compose -f deploy/docker/docker-compose.all.yml up -d --build`
- 导出产物本地保存验证通过
- `/api/live` SSE 实时推送验证通过

## 交付建议

如果继续往前做，建议顺序：

1. 继续增强断线消息补齐
2. 拆分导出 worker
3. 客户确认后再评估是否恢复 Agent 前端入口
